# ============================================================================
# SECURITY: npm configuration for dependency governance
# ============================================================================

# Block installation if npm audit finds high/critical vulnerabilities
audit-level=high

# Pin exact versions to prevent unexpected updates via semver ranges
save-exact=true
