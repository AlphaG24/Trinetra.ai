# TRINETRA AI — ANTIGRAVITY EXECUTION RULES
# Version: 2.0.0
# This file must be read BEFORE any code is generated.

---

## 🔴 FIRST: ALWAYS READ THESE FILES FIRST

Before generating ANY code, you MUST read:
1. MASTER_PLAN.md — Complete architecture, database schema, routes, phases
2. PROGRESS.md — Current progress, what's done, what's next

---

## 🔴 CRITICAL RULES (ZERO VIOLATIONS)

### 1. SINGLE SOURCE OF TRUTH
- Every concept exists EXACTLY once
- Check MASTER_PLAN.md Section 3.2 for existing tables before creating ANY table
- If you find duplication → MERGE, never create parallel structures

### 2. DATABASE RULES
- All tables are defined in MASTER_PLAN.md Section 3.2
- Use Supabase CLI for ALL schema changes: `supabase db diff -f [name]`
- NEVER write raw SQL directly to production database
- Every table MUST have Row Level Security (RLS)
- Every query MUST include tenant isolation (organization_id or user_id)
- PII fields (phone, email, gstin) MUST be encrypted via encrypt_pii()

### 3. SECURITY RULES (IMMEDIATE REJECTION IF VIOLATED)
- NO hardcoded API keys, tokens, passwords, or secrets in ANY code
- NO .env files committed to git (only .env.example allowed)
- ALL secrets from process.env or system_config table
- Admin operations: verify profiles.role IN ('admin', 'super_admin')
- Super admin operations: verify profiles.role = 'super_admin'
- ALL admin actions logged to audit_logs table
- NEVER log sensitive data (phones, emails, keys, passwords) to console
- Check MASTER_PLAN.md Section 16 for complete .gitignore

### 4. FILE ORGANIZATION (STRICT)
- Admin pages → frontend/src/app/(admin)/admin/
- Dashboard pages → frontend/src/app/(dashboard)/dashboard/
- Marketing pages → frontend/src/app/(marketing)/
- API routes (Next.js) → frontend/src/app/api/
- API routes (FastAPI) → backend/app/routes/
- Components → frontend/src/components/{category}/
- Database migrations → supabase/migrations/ (auto-generated by CLI)

### 5. DOMAIN & ROUTING (NEVER CHANGE)
- Primary domain: trinetraedu-ai.com (NOT trinetraai.com)
- Client dashboard: app.trinetraedu-ai.com → routes to /dashboard
- Admin dashboard: admin.trinetraedu-ai.com → routes to /admin
- API backend: api.trinetraedu-ai.com (FastAPI on Render)
- All in ONE Next.js project — subdomains handled by Vercel rewrites (vercel.json)

### 6. TECH STACK (DO NOT DEVIATE)
- Frontend: Next.js 14 App Router, TypeScript, Tailwind, shadcn/ui
- Backend: FastAPI (Python 3.11+)
- Database: Supabase PostgreSQL with RLS
- Auth: Supabase Auth (JWT in cookies, SSR)
- TTS Hindi/Hinglish: Sarvam AI (best quality)
- TTS English: ElevenLabs
- STT Hindi/Hinglish: Sarvam AI
- STT English: Deepgram
- LLM: Gemini 2.0 Flash (free tier, 1500 req/day)
- Voice cloning: ElevenLabs API

### 7. NAMING CONVENTIONS (CONSISTENT)
- Components: PascalCase — VoiceDemo.tsx, LeadCard.tsx
- API routes: kebab-case — /voice-calls, /agent-templates
- Database tables: snake_case — voice_calls, agent_health_logs
- Database columns: snake_case — created_at, organization_id
- TypeScript functions: camelCase — getUserOrgId, isAdmin
- Python functions: snake_case — get_user_org_id, is_admin

### 8. CODE QUALITY STANDARDS
- TypeScript: Strict types, no 'any' unless absolutely necessary
- Python: Type hints on ALL function parameters and returns
- Components: Follow existing shadcn/ui + Tailwind patterns
- API Routes: try-catch with proper HTTP status codes (200, 400, 401, 403, 404, 500)
- Database: Parameterized queries ($1, $2), NEVER string interpolation

### 9. AFTER EVERY CODE GENERATION (MANDATORY)
You MUST do these 3 things:
1. Update PROGRESS.md — mark completed tasks, note what's next
2. List ALL files modified/created in your response
3. List ALL security checks performed

### 10. NEVER DO THIS (REJECTED IMMEDIATELY)
- Create parallel tables for the same concept
- Skip RLS on new tables
- Hardcode any secret or key
- Use raw SQL without migration files
- Create new auth systems (use Supabase Auth only)
- Change the primary domain from trinetraedu-ai.com
- Skip tenant isolation in any database query
- Create new .env files (use existing .env.local which is gitignored)
- Put sensitive data in component state without encryption

### 11. SUPER ADMIN INFO
- Super admin email is in profiles table with role='super_admin'
- Only super_admin can access /admin/system routes
- Only super_admin can rotate API keys
- Do NOT hardcode any email — always check profiles.role

### 12. PROGRESS TRACKING (MANDATORY)
- Read PROGRESS.md before starting any task
- Update PROGRESS.md after completing any task
- Mark phase as IN_PROGRESS when starting
- Mark phase as COMPLETED when done
- Add date completed to each task
- If blocked, note BLOCKED with reason

---

## 📋 PRE-CODE CHECKLIST (Run Mentally Every Time)

Before outputting ANY code:
□ Did I read MASTER_PLAN.md for this section?
□ Did I read PROGRESS.md for current phase?
□ Does this file/table/route already exist?
□ Is there RLS policy for this database operation?
□ Are all API keys/secrets from environment variables?
□ Is tenant isolation enforced in this query?
□ Are admin operations properly gated by role?
□ Will this fit the existing project structure?
□ No sensitive data in logs or error messages?
□ Did I use the correct naming convention?

## 📋 POST-CODE CHECKLIST (Run Mentally Every Time)

After outputting code:
□ Did I update PROGRESS.md?
□ Did I list all files modified/created?
□ Did I list all security checks performed?
□ Did I note any warnings for the next phase?